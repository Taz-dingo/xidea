import type { WorkspaceData } from "@/app/workspace/hooks/use-data";

export function useFixtureSync({
  data: _data,
  fixtureIdFromUrl: _fixtureIdFromUrl,
}: {
  data: WorkspaceData;
  fixtureIdFromUrl: string | null;
}): void {
  return;
}
